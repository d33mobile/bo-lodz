{
  "name": "bo-lodz",
  "version": "1.0.0",
  "description": "BO Łódź — budżet obywatelski browser",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "e2e": "playwright test",
    "coverage:e2e": "playwright test"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@playwright/test": "^1.60.0",
    "@vitest/coverage-v8": "^2.0.0",
    "monocart-coverage-reports": "^2.12.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.0",
    "vitest": "^2.0.0"
  }
}
